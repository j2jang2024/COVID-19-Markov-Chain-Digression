class Human:
    def __init__(state,city,friends,awareness):
        self.state = state
        self.city = city
        self.friends = friends
        self.awareness = awareness
    
    def oneDay():
        # e-> i: 0.05
        if self.state = 'e':
            if random()<0.05:
                self.state = 'i'
        #i -> r: 0.1
        if self.state = 'i':
            if random()<0.1:
                self.state = 'r'



John = Human('s','Seoul',[],0)